'use strict'

/*
    onQuit
    Listens for players leaving the Discord server
*/
module.exports = class {
    constructor(client, db, handlers) {
        /*
            Removed for now
            client.on('guildMemberRemove', member => {});
        */
    }
}