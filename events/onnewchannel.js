'use strict'

/*
    onNewChannel
    Listens for new channels being created
*/
module.exports = class {
    constructor(client, db, handlers) {
        /*
            Removed for now
            client.on('channelCreate', member => {});
        */
    }
}